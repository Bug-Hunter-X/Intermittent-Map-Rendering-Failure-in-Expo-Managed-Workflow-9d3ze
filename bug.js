This bug occurs when using the Expo managed workflow with the react-native-maps library.  The map sometimes fails to render, showing only a blank space. This happens inconsistently, making it difficult to debug.